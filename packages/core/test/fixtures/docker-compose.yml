version: "3.8"

services:
  web:
    image: nginx:1.25.3
    ports:
      - "80:80"

  db:
    image: postgres:16.1
    environment:
      POSTGRES_PASSWORD: secret

  cache:
    image: redis:7.2.3-alpine
